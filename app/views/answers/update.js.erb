(() => {
  const answerErrors = document.querySelector('.answers .answer__errors')
  answerErrors.innerHTML = '<%= render 'shared/errors', resource: @answer %>'

  <% unless @answer.errors.present? %>
    const answers = document.querySelector('.answers')
    answers.innerHTML = '<%= j render @question.answers %>'
  <% end %>
})()